{
  "name": "solution",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "playwright test",
    "check": "node scripts/check-contract.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
