{
  "name": "solution",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node server.js",
    "test": "playwright test",
    "check": "node scripts/check-contract.js",
    "prepare": "node scripts/install-hooks.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "canvas": "^3.2.3",
    "playwright": "^1.60.0",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "vite": "^8.0.16"
  }
}
